<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:converter="clr-namespace:Safe.PL.ViewModel.Convertors"
             mc:Ignorable="d"
             x:Class="Safe.PL.NotesListPage">
    <ContentPage.Resources>
        <converter:ElapsedTimeToStringConvertor x:Key="timeConvertor"/>
    </ContentPage.Resources>

    <ContentPage.ToolbarItems>
        <ToolbarItem x:Name="addNoteButton" Text="Add" Clicked="AddNoteButton_Clicked"/>
    </ContentPage.ToolbarItems>

    <StackLayout VerticalOptions="Center">
        <ListView x:Name="notesListView">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextCell Text="{Binding Title}" 
                              Detail="{Binding LastModifiedTime, Converter={StaticResource timeConvertor}}">
                        <TextCell.ContextActions>
                            <MenuItem Text="Delete" Command="{Binding DeleteNoteCmd}"/>
                        </TextCell.ContextActions>
                    </TextCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>
</ContentPage>